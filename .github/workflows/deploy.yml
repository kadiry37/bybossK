name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:
  repository_dispatch:
    types: [content-update]

# IMPORTANT: Only one deploy at a time. If a new one starts, cancel the previous.
concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./astro-project
        
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: './astro-project/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Create .env file for Backend
        working-directory: .
        run: |
          echo "DB_HOST=localhost" > ./php-backend/.env
          echo "DB_NAME=${{ secrets.DB_NAME }}" >> ./php-backend/.env
          echo "DB_USER=${{ secrets.DB_USER }}" >> ./php-backend/.env
          echo "DB_PASS=${{ secrets.DB_PASS }}" >> ./php-backend/.env
          echo "GITHUB_TOKEN=${{ secrets.GITHUB_PAT }}" >> ./php-backend/.env
          echo "GITHUB_REPO_OWNER=kadiry37" >> ./php-backend/.env
          echo "GITHUB_REPO_NAME=bybossK" >> ./php-backend/.env

      - name: Upload Backend to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: domains/bybossmimarlik.com/public_html/
          local-dir: ./php-backend/
          port: 21
          security: loose
          timeout: 600000
          state-name: .ftp-backend-sync-state.json
          dry-run: false
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/uploads/**
            **/github_webhook.log
            **/.ftp-deploy-sync-state.json

      - name: Add hosts entry for API mapping
        run: |
          echo "104.247.162.226 bybossmimarlik.com" | sudo tee -a /etc/hosts

      - name: Build Astro
        run: npm run build
        env:
          API_URL: ${{ secrets.API_URL }}
          NODE_ENV: production
          NODE_TLS_REJECT_UNAUTHORIZED: '0'

      - name: Remove placeholder pages
        run: |
          find dist -type d -name '_placeholder' -exec rm -rf {} + 2>/dev/null || true

      - name: Upload Frontend to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: domains/bybossmimarlik.com/public_html/
          local-dir: ./astro-project/dist/
          port: 21
          security: loose
          timeout: 600000
          state-name: .ftp-deploy-sync-state.json
          dry-run: false
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/_placeholder/**

      - name: Ping IndexNow
        run: |
          echo "Pinging IndexNow API..."
          curl -s -k -X GET "https://bybossmimarlik.com/api/indexnow.php"

